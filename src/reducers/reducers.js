/* eslint_disable */
import React from 'react'
import { v4 } from 'uuid'
import C from '../actions/constants'


// Exported Reducer:

export const lastFeedbackStatus = (state=[], action) =>{
    switch (action.type){
      default:
        return state;
    }
}


// Exported Reducer:
export const savedExpressions = (state=[], action) =>{
    switch (action.type){
      default:
        return state;
    }
}

// Internal Use Reducer:
